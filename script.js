// The given sample JavaScript code
console.log("Hello, World!");
let name = "Student";
console.log("Welcome, " + name);

// I still don't get what it means by linking it, but here's the output linked to the HTML file
document.getElementById("output1").innerHTML = "Hello, World!";
document.getElementById("output2").innerHTML = "Welcome, " + name;
